<form method="post" action="{{ path('app_validate_order_delete', {'id': validate_order.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ validate_order.id) }}">
    <button class="btn btn-danger btn-sm mb-2">Delete</button>
</form>
