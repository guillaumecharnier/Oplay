{% extends 'base.html.twig' %}

{% block title %}Edit Game{% endblock %}

{% block body %}
    <div class="d-flex justify-content-center">
        <div class="w-50">
            <h1 class="text-center">Edit Game</h1>

            {{ include('backoffice/game/_form.html.twig', {'button_label': 'Update'}) }}

            <div class="text-center mt-3">
                <a href="{{ path('app_game_index') }}" class="btn btn-primary btn-sm">Back to list</a>

                {{ include('backoffice/game/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
