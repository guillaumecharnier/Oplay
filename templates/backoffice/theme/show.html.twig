{% extends 'base.html.twig' %}

{% block title %}Theme{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-lg-6">
                <h1 class="text-center">Theme</h1>

                <table class="table">
                    <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ theme.id }}</td>
                        </tr>
                        <tr>
                            <th>Name</th>
                            <td>{{ theme.name }}</td>
                        </tr>
                    </tbody>
                </table>

                <div class="text-center mt-3">
                    <a href="{{ path('app_back_theme_index') }}" class="btn btn-secondary">Back to list</a>
                    <a href="{{ path('app_back_theme_edit', {'id': theme.id}) }}" class="btn btn-warning">Edit</a>
                    {{ include('backoffice/theme/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
